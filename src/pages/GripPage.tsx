import { useState } from "react";import { useNavigate } from "react-router-dom";import { CameraStage } from "../components/CameraStage";
export function GripPage(){const nav=useNavigate();const [seen,setSeen]=useState(false);return <main className="app-shell grip"><header className="page-head"><button className="back" onClick={()=>nav(-1)}>‹</button><h1>握笔检测 <small>Beta</small></h1><span /></header><section className="grip-guide"><span>✎</span><h2>给手和笔一个近景</h2><p>从惯用手一侧前方约 30°–45° 拍摄，让手掌、拇指、食指和笔清楚进入画面。</p></section><CameraStage kind="hand" onHand={(r)=>setSeen(r.landmarks.length===1)}/><section className="tip-card"><span>实验性辅助提示</span><p>{seen?"已看到一只手。请把手和笔放在取景框中央，保持自然放松。":"请把一只手和笔靠近镜头，画面保持明亮。"}</p></section><p className="privacy-line">这是辅助提醒，不判断“标准握笔”。</p></main>}
